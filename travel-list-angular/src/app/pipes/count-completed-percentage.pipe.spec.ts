import { CountCompletedPercentagePipe } from './count-completed-percentage.pipe';

describe('CountCompletedPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new CountCompletedPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
